# Dungeon 런타임 아키텍처 설계 논의 문서

> 상태: **논의 중 (Draft)** — 아직 코드로 옮기지 않음. 아래 항목들에 대해 서로 합의가 되면 그때 파일 단위로 쪼개서 구현 논의를 시작한다.
> 작성 목적: 지금까지 대화로 나온 결정/방향을 한 곳에 정리해서, 빠진 부분이나 아직 안 정한 부분을 눈으로 보고 다음 논의를 이어가기 위함.

---

## 0. 배경 / 왜 이 문서가 필요한가

맵 제네레이터(FloorLayoutGenerator, FloorTilemapPainter, Preview 툴들)는 매번 세부사항을 같이 맞춰가며 만들었고, 그 결과물에 대해 "이해하고 있다"는 확신이 있었다.

반면 Tick / 이동 / 카메라 추적 4개 스크립트는 질문 3개에 대한 답을 듣고 바로 완성된 코드로 만들어버렸고, 그 결과 "구조를 이해 못 하는데 무슨 의미냐"는 피드백을 받았다. 그 스크립트들은 삭제(정확히는 `_to_delete/`로 이동)했다.

이번엔 순서를 바꾼다: **먼저 구조를 글로 정리하고 합의 → 그 다음 파일 단위로 쪼개서 하나씩 구현**. 이 문서는 그 첫 단계다. 코드는 한 줄도 없다.

---

## 1. Tick / 턴-속도 시스템

### 1.1 스코프: Dungeon 전용

- **BaseCampScene**: Tick 없음. 실시간(real-time) 이동. 다키스트 던전의 "Estate" 화면처럼 준비 단계 개념.
- **DungeonScene**: Tick 있음. 10개 층이 전부 이 씬 안에 동시에 존재.

→ Tick 시스템은 **DungeonScene 진입 시에만 살아있는 존재**로 봐야 한다. BaseCamp에서는 TickManager 자체가 필요 없거나, 최소한 비활성 상태여야 한다.

**미정 질문 A**: TickManager를 씬 전환마다 새로 만드는가(DungeonScene 로드 시 생성, 언로드 시 파괴), 아니면 항상 존재하되 BaseCamp에서는 그냥 아무도 안 쓰는 상태로 놔두는가? 후자면 10층을 씬 하나에 다 올려놓는 구조와 자연스럽게 맞물릴 수 있음(씬 자체를 안 내렸다 올렸다 하니까).

### 1.2 이벤트 구독 모델

플레이어(및 다른 엔티티들)가 static 카운터를 폴링하는 게 아니라, **TickManager가 이벤트를 발행하고 엔티티들이 구독**하는 방식.

```
개념적 흐름 (의사코드 아님, 그냥 구조 설명):
TickManager
  └─ OnTick 이벤트를 들고 있음
  └─ 누군가 행동을 소비하면(플레이어 이동/공격 등) 그만큼 시간을 진행시킴
  └─ 시간이 특정 엔티티의 "다음 행동 시점"을 넘으면 그 엔티티에게 알림

Player, Enemy 등
  └─ 자신의 speed 스탯을 가짐
  └─ TickManager에 자신을 등록(구독)
  └─ 행동을 하면 "이 행동에 걸리는 시간 비용"을 TickManager에 보고
```

**미정 질문 B**: 이벤트가 "매 틱마다 전체 브로드캐스트"(모두가 OnTick을 받고 각자 "내 차례인지" 판단)인지, 아니면 "다음 차례인 엔티티에게만 콜백"(TickManager가 스케줄러 역할, 큐를 들고 다음 차례를 직접 호출)인지. 녹픽던 방식은 후자에 가까움(Actor 큐 + 우선순위).

### 1.3 속도/시간 비용 모델 (녹픽던 참고)

녹픽던(Shattered Pixel Dungeon) 기준 확인된 사실:

- 기본 이동 속도(travel speed) = 1 (즉, 1타일 이동 = 1턴 소모)
- 기본 공격 속도(attack speed) = 1 (1회 공격 = 1턴 소모)
- 배율(multiplier)로 시간 비용이 줄어드는 구조. 예:
  - Freerunner 클래스: 이동 속도 1.6배 → 이동에 걸리는 시간이 1/1.6로 줄어듦
  - 특정 적(Crazy Thief, Bandit 등): 공격당 0.5턴 소모 → 사실상 플레이어 한 턴에 2번 공격
  - Ring of Haste: 플레이어 쪽 대표적인 가속 아이템

즉 "행동 1회 = 고정 -1"이 아니라, **"행동 1회 = 기본 비용 × (1 / 속도배율)"** 형태의 시간 소모 모델. 우리 프로젝트도 이 방향(고정 비용이 아니라 배율 기반)으로 간다는 게 지금까지 나온 의도.

**미정 질문 C**: 우리 게임에서 "속도가 빠르다"를 스탯으로 어떻게 표현할지 — 배율(1.6배 같은 곱연산) 하나만 쓸지, 행동별로 다른 비용(이동 비용 vs 공격 비용을 따로 관리)을 둘지. 녹픽던은 이동/공격 속도를 분리해서 관리함 — 우리도 분리할지, 아니면 처음엔 하나의 speed 스탯으로 통합해서 단순하게 시작할지.

**미정 질문 D**: 적(enemy) AI도 같은 TickManager를 구독하는 구조로 갈지 확정 필요 (지금까지 뉘앙스상 "Dungeon에 있는 모든 행동 주체는 Tick을 구독"하는 쪽으로 보임 — 맞는지 확인 필요).

---

## 2. 10개 층 사전 생성 + 페이드 전환 흐름

### 2.1 왜 미리 다 만드는가

명확한 이유: **층 이동 시 로딩 끊김 없이 페이드 인/아웃만으로 자연스럽게 전환**하기 위해서. 씬을 다시 로드하면 그 순간 로딩 텀이 생기는데, 페이드 연출과 로딩이 겹치면 매끄럽지 않음. 그래서 "무거운 생성 작업"을 미리 다 끝내놓고, 실제 층 이동 시점에는 카메라/플레이어 위치만 옮기면 되게 만드는 것.

### 2.2 두 종류의 "다음"이 있다 — 구분이 핵심

| 구분 | 트리거 | 실제로 일어나는 일 |
|---|---|---|
| **다음 층** | 계단에서 "다음 층" 선택 | **씬 이동 아님.** 같은 DungeonScene 안에서 카메라 + 플레이어 위치를 다음 층 좌표로 옮김. 페이드 아웃 → (위치 이동) → 페이드 인. |
| **탈출** | 계단에서 "탈출" 선택 | **진짜 씬 이동.** `SceneManager`로 DungeonScene → BaseCampScene. 페이드 아웃 → 씬 로드 → 페이드 인. |

- 계단에 도달하면 UI로 "탈출" / "다음 층" 둘 중 하나를 플레이어에게 물어봄.
- **어느 쪽을 눌러도 페이드 인/아웃 연출 자체는 공통**으로 나감. 다만 페이드가 어두운 동안 뒤에서 하는 작업이 다름(카메라 이동 vs 씬 로드).

### 2.3 구조적으로 봤을 때

```
DungeonScene
  ├─ Floor 1 (미리 생성됨, 특정 world-space 영역에 배치)
  ├─ Floor 2 (미리 생성됨, Floor 1과 겹치지 않는 다른 world-space 영역)
  ├─ ...
  └─ Floor 10

한 시점에 "현재 층"만 카메라가 비추고, 나머지 층은 화면 밖에 존재.
층 이동 = 카메라(+플레이어)를 다음 층의 world-space 영역으로 순간이동.
```

**미정 질문 E**: 층 사이 배치를 뭘로 할지 — 예전 삭제된 `DungeonRunManager.cs`는 X축으로 층을 나란히 늘어놓는 방식이었는데(이것도 "급조" 지적을 받은 부분), 다시 검토가 필요. X축 나열이 제일 단순하긴 하지만, 이게 "진짜 원하는 구조"였는지 아니면 그냥 빨리 동작하게 만들려고 고른 임시방편이었는지 확인 필요.

**미정 질문 F**: 페이드 인/아웃 연출 자체(지속시간, 색상, UI 레이어 처리 등)는 아직 전혀 얘기 안 함 — 이건 나중 단계(비주얼 폴리싱)로 미뤄도 되는지, 아니면 지금 구조 설계에 포함해야 하는지.

**미정 질문 G**: "탈출"을 선택했을 때 인벤토리/획득 아이템 등 "런(run)" 데이터를 어떻게 BaseCamp로 넘길지 — 씬이 실제로 바뀌므로 정적(static) 매니저나 별도 영속 오브젝트가 필요할 가능성이 있음. 아직 논의 전.

---

## 3. 입력 방식: 마우스 좌클릭 이동

- 키보드(WASD/방향키)가 아니라 **마우스 좌클릭으로 이동**. 녹픽던 방식을 그대로 참고.
- 이전에 삭제한 `GridMover.cs`는 방향키 기반 한 칸씩 이동이었는데, 이 부분이 이번 방향과 안 맞음 — 그래서 폐기 대상에 포함됐던 것.

### 3.1 녹픽던 방식 요약 (참고용, 우리가 그대로 따를지는 별개)

- 클릭한 타일까지 자동 경로탐색(pathfinding)으로 이동
- 이동 한 칸 = Tick 시스템에서의 행동 1회로 취급, 위 1장의 속도/비용 모델을 그대로 적용받음
- 이동 도중 적이 시야에 들어오는 등 상황이 바뀌면 자동 이동이 중단되는 형태(녹픽던 기준)

**미정 질문 H**: 우리도 "클릭 → 경로탐색 → 여러 칸 자동 이동"까지 갈지, 아니면 1단계로는 "클릭한 인접 칸 1칸만 이동"으로 단순하게 시작할지. 경로탐색까지 가면 A* 같은 별도 알고리즘 + "이동 중 상황 변화 감지" 로직이 필요해서 범위가 커짐.

**미정 질문 I**: 공격은 어떻게 트리거되는지 — 적이 있는 칸을 클릭하면 자동으로 공격으로 전환되는지(녹픽던 방식), 아니면 별도 입력이 있는지.

---

## 4. 지금까지 나온 미정 질문 모음 (다음 논의용 체크리스트)

1. TickManager를 씬 전환마다 새로 만들지, 상시 존재시키되 BaseCamp에서는 비활성으로 둘지
2. Tick 이벤트가 전체 브로드캐스트인지, 큐 기반으로 다음 차례에게만 콜백하는지
3. 속도 스탯을 이동/공격으로 분리할지, 하나로 통합해서 시작할지
4. 적(enemy)도 같은 TickManager를 구독하는 게 맞는지
5. 10개 층을 world-space 상에 어떻게 배치할지 (X축 나열이었던 기존 방식 재검토)
6. 페이드 연출 세부사항을 지금 정할지 나중으로 미룰지
7. "탈출" 시 런 데이터(아이템, 진행상황 등)를 BaseCamp로 어떻게 넘길지
8. 이동을 경로탐색 기반 자동 이동으로 만들지, 인접 1칸 클릭 이동으로 단순화할지
9. 공격 트리거 방식 (적 칸 클릭 = 자동 공격인지)

이 중에서 어떤 걸 먼저 정하고 싶은지, 아니면 순서 상관없이 훑으면서 하나씩 정할지는 사용자 판단에 맡김.
