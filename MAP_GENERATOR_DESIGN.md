# 맵 제너레이터 설계 논의 문서

> 상태: **구조 설계 확정 (A~F + 방 태깅 + 잠금 메커니즘 H까지 전부 완료)** — 코드 없음. `DUNGEON_RUNTIME_ARCHITECTURE.md`, `ITEM_SYSTEM_DESIGN.md`와 같은 패턴: 먼저 구조를 글로 합의 → 그 다음 구현.
> 배경: 홈PC 쪽 대화에 `FloorLayoutGenerator`, `FloorTilemapPainter` 같은 이름이 이미 등장했었지만, 이 문서는 그것과 별개로 노트북 대화에서 새로 설계를 시작한 것 — 나중에 두 대화 내용을 맞춰볼 필요가 있음(9장 참고).
> 목표: "맵 제너레이터가 아직 실제 base를 못 잡았다"는 문제를 좁혀서, 생성 알고리즘 + 사용할 스프라이트를 구체화하기.

---

## 1. 생성 알고리즘 (방향 확정)

**방(Room) + 복도(Corridor) 절차생성**으로 확정. 구체적인 구현 방식 후보:

- **BSP(Binary Space Partition) 트리 분할** — 맵 영역을 재귀적으로 쪼갠 뒤 각 리프 영역에 방을 하나씩 배치, 부모-자식 관계를 따라 복도로 연결. 녹픽던 계열 로그라이크의 표준 방식.
- 대안(랜덤 배치 + MST 연결)도 있지만, 지금까지의 논의 패턴(레퍼런스로 계속 녹픽던을 참고해온 것)을 고려하면 **BSP가 기본 후보**로 제안.

**확정 (질문 A 해결)**: **BSP로 확정**. 방 형태/배치/복도 연결은 BSP가 절차적으로 처리하고, 방 역할(상자방/열쇠방/보스방 등)은 BSP 결과 위에 얹는 별도 태깅 패스로 처리 — 이 태깅 로직은 4장 "아직 다루지 않은 것"의 방 종류 구분 항목에서 별도 설계 예정.

Cellular Automata는 지금 채택하지 않지만, **나중에 특수 층(예: 동굴형 테마 층)을 위한 별도 실험 대상으로 메모** — 학습 겸 시도해볼 여지는 남겨둠.

> 참고: 과거 프로젝트에서 stage 0/1/2/3처럼 씬을 층마다 손으로 제작하다가 부담이 컸던 경험이 있어, 절차적 생성(BSP) 쪽으로 확실히 방향을 잡음 — 프리팹 방식은 특정 방(보스방 등)만 예외적으로 손으로 심는 하이브리드로 부분 도입하는 정도로 제한.

## 2. Tick 시스템 확정 모델 (원 설계 대화에서 확인 — 매우 중요, 이전 추정 대체)

이전에 제가 "1 tick = 6타일", "tick = 카운트다운 자원(추정)" 이라고 짚었던 건 **틀린 추정이었고**, 사용자가 실제 설계 당시 정했던 확정 수치를 알려주셔서 정정합니다. 이게 맵 규격 논의의 진짜 기준입니다.

### 설계 의도 (확정)

시간(실시계)을 tick과 연동시킨 이유는 단순히 "탈출 타이머"만이 아니라, **경과 시간에 따라 몹이 강해지고 다른 이벤트가 발생하게 해서 난이도와 깊이를 동시에 높이려는 의도**임. 즉 이 게임의 위험도는 **두 축으로 동시에 상승**하는 구조:
1. **층 깊이** — 몇 층까지 내려갔는지
2. **경과 시간(밤 등)** — 같은 층에 있어도 시간이 흐르면 더 위험해짐

이 두 축이 서로 독립적이라, "얕은 층에서 시간을 오래 끄는 것"과 "깊은 층으로 빨리 내려가는 것" 둘 다 나름의 리스크가 생기는 균형 구조를 만들 수 있음 — 오래 버티기만 해도 안전하지 않다는 점이 익스트랙션 장르의 핵심 압박과 잘 맞음.

### 확정 수치
- **1 Tick = 게임 내 5분 = 이동 5타일** (제가 이전에 6타일이라 했던 건 오기, **5타일이 맞음**)
- **던전 최대 체류시간 = 288 Tick = 24시간** — 이 수치를 넘기면(정확히는 288 Tick 사용 시점) **MIA(실종) 처리**. 이게 바로 익스트랙션 장르의 "탈출 타이머"이자, `ITEM_SYSTEM_DESIGN.md` 9장에 미정으로 남아있던 "시작 tick 값"의 정답이었음 — **시작 tick = 288으로 확정**, 별도로 상향할 필요 없음 (스왑 1tick 비용은 이미 이 288 예산 안에서 소화되는 전제로 설계된 것으로 보임)
- **행동하지 않으면 시간이 흐르지 않음** — 메뉴를 보거나 고민하는 시간은 tick 소모 없음. 이동/공격/루팅 같은 실제 명령을 실행해야 tick이 진행됨
- **UI 구상**: 남은 tick 숫자보다 **실시계(예: 21:35) + 남은 시간**을 같이 보여주는 방향
- 출격 시각(예: 06:00) 기준 예시: 72tick 사용 시 12:00, 144tick 사용 시 18:00, 216tick 사용 시 00:00, 288tick 사용 시 다음날 06:00 = MIA
- **미확정 세부사항**: 0시를 마감 기준으로 할지, 출격 시각부터 정확히 24시간을 잴지는 아직 조정 여지 있음 (원 설계 당시에도 미정이었다고 하심)

### 이게 맵 규격에 주는 의미

1 tick = 5타일이므로 이동 비용은 **타일당 0.2 tick**. 6타일 기준(0.167 tick/타일)과 큰 차이는 아니지만, **288 tick이라는 전체 예산이 못박혀 있다는 게 핵심**입니다. 이제 규격은 "느낌"이 아니라 이 예산에서 역산할 수 있습니다.

- 288 tick 전액을 이동에만 쓴다면 이론상 288 × 5 = **1440타일**까지 이동 가능하지만, 실제로는 전투/스왑/아이템 사용에도 tick이 들어가므로 순수 이동 가용분은 이보다 훨씬 적음

### 확정: "10층 완주"는 정석이 아님 + 공격 tick 비용

- **10층을 전부 밟는 것은 의도된 정석 루트가 아님** — 대부분 중간에 탈출하는 게 기본값. 288tick 예산은 "완주 가능하게" 잡는 게 아니라 오히려 **깊이 갈수록 빠듯해지도록** 설계된 것으로 확정
- **기본 공격 = 1 tick (확정)** — 이동(5타일=1tick), 스왑(1tick)과 동급. tick의 "가치"를 의도적으로 높게 잡음 — 공격 한 번이 이동 5칸과 같은 비중이라는 뜻
- **스킬은 스킬마다 X/Y/Z tick으로 서로 다른 비용을 가짐** (확정) — 무기 스킬 시스템(별도 엑셀) 쪽에서 각 스킬에 데미지/범위뿐 아니라 **tick 비용**도 함께 정의해야 함. 강한 스킬일수록 비싼 tick을 쓰는 트레이드오프 구조로 보임

이걸로 층당 예산을 대략 역산하면: 이동 15~20tick(24x24 그리드) + 전투 2~4회 × 회당 공격 3~6회(3~6tick) = **층당 대략 25~45tick**. 288tick 예산으로 나누면 6~11개 층 분량이 이론상 나오지만, 스킬 사용·아이템 사용·귀환 여유분까지 감안하면 **10층 완주가 사실상 불가능한 빠듯한 예산**이라는 설계 의도와 정확히 맞아떨어짐

**확정**: 포션/스크롤 사용, 상자/문 개방 등 그 외 행동도 **전부 1 tick으로 통일** — "각 행동의 가치를 계산해보면 1tick이 적당하다"는 판단, 실제 변수 창출/수익 구조 측면에서도 합리적이라고 확인됨. 즉 **이동(5타일) / 공격 / 스왑 / 아이템 사용 / 상호작용 — 전부 기본 단위는 1 tick**으로 통일된 셈

**확정 (질문 C 해결)**: 보스/엘리트를 제외한 일반 전투는 **10 tick 이내에 종료**되도록 설계. 단, 플레이어 장비가 해당 층 몹 수준에 못 미치면 전투가 이보다 길어지거나 최악의 경우 사망 — **이 격차가 의도된 난이도 상승 레버**임 (몹을 더 강하게 만드는 게 아니라, "장비가 안 맞으면 오래 걸리고 위험해진다"는 상대적 구조). 몹 스탯 설계 시 이 10tick 기준(=공격 최대 10회 교환)을 상한으로 역산하면 됨.

### v4 그리드 제안 (288tick 예산 기준으로 재조정)

| 항목 | v4 제안값 | 비고 |
|---|---|---|
| 그리드 크기 | 24 x 24 타일 | 순수 이동으로 완전탐색해도 대략 15~20tick 수준 — 나머지 예산은 전투/스왑에 배정 |
| 방 개수 | 층당 5~8개 | |
| 방 크기 범위 | 3x3 ~ 7x7 타일 | |
| 복도 폭 | 1~2 타일 | |

**확정 (질문 D 해결)**: 방 크기/개수는 층 구간에 따라 나뉨.

- **1~5층**: 위 v4 제안(24x24, 방 5~8개, 3x3~7x7)보다 **작게** 축소 — 초반 층은 상대적으로 가볍게 탐색 가능하게, 장비 준비/자금 축적 구간으로 기능
- **6층 이상**: 맵 제너레이터가 낼 수 있는 **"진짜" 풀 사이즈**로 전환 — v4 제안값(24x24, 방 5~8개, 3x3~7x7)이 사실상 이 6층+ 구간의 기준선이 됨. 여기서부터 그리드/방 개수를 더 키우는 것도 검토 가능
- **설계 의도(확정)**: 6층 이상부터는 **제대로 된 준비(장비/포션/스크롤) 없이 288tick 예산 안에 탐험하는 것 자체가 불가능**하도록 규격을 잡음 — 즉 방 크기/개수 확대가 "이동 tick 소모 증가"를 통해 자연스럽게 긴장감 곡선을 만드는 구조가 맞음. 1~5층에서 벌어들인 자원으로 6층 이상을 준비하지 않으면 무리라는 게 핵심 설계 압박.
- **미확정 세부수치**: 1~5층 구간 내에서도 층마다 점진적으로 커질지, 아니면 1~5층은 고정 소형 규격 하나로 통일할지는 아직 안 정함(추후 조정)

## 3. 타일 ↔ 스프라이트 매핑 (0x72 원본에서 직접 확인)

0x72 DungeonTilesetII 원본 스프라이트를 확인한 결과, 맵 생성에 바로 쓸 수 있는 타일이 이미 충분히 있음:

| 용도 | 스프라이트 |
|---|---|
| 바닥 (변형 타일) | `floor_1` ~ `floor_8` (8종, 랜덤 배치로 시각적 변주) |
| 계단(다음 층) | `floor_stairs` |
| 사다리 | `floor_ladder` (현재 미사용 — "다음 층 직행" 스크롤 전용으로 예약, 아래 확정 내용 참고) |
| 함정 | `floor_spikes_anim_f0~f3` (4프레임 애니메이션) |
| 벽 - 직선형 | `wall_top_left/mid/right`, `wall_left/mid/right` |
| 벽 - 모서리/연결부 | `wall_edge_top_left`, `wall_edge_mid_left`, `wall_edge_bottom_left`, `wall_edge_tshape_*` 등 — **Rule Tile(오토타일) 세팅에 맞게 설계된 조각들로 보임.** Unity Tilemap의 Rule Tile 기능을 쓰면 벽 배치를 자동으로 올바른 모서리 조각으로 채울 수 있음 |
| 문 | `doors_frame_left/right/top`(문틀) + `doors_leaf_closed`/`doors_leaf_open`(문짝, 별개 오브젝트로 열고 닫힘 표현 가능) |
| 장식(선택적) | 분수(`wall_fountain_*`, 파랑/빨강 애니메이션), 배너(`wall_banner_*`), 벽 구멍(`wall_hole_1/2`), `wall_goo`/`wall_goo_base` — 방 안 랜덤 배치용 |

**확정 (질문 E 해결)**: 현재 맵 제너레이터는 **`floor_stairs`만 사용**. `floor_ladder`는 지금 당장은 미사용 — 추후 추가될 **"다음 층 직행" 스크롤**(가칭) 전용으로 예약해둠. 이 스크롤은 현재 층에서 즉시 다음 층으로 이동시켜주지만, **탈출은 지원하지 않음** (`ITEM_SYSTEM_DESIGN.md`에 이미 확정된 "강제 탈출 스크롤"과는 명확히 다른 별개 아이템). ⚠️ 이건 `ITEM_SYSTEM_DESIGN.md`의 스크롤 목록(현재 4종 확정)에 대한 잠재적 추가 항목이라, 저쪽 문서에도 반영할지는 별도로 확인 필요 — 아래 "다음 액션" 참고.

**확정 (질문 F 해결)**: **Rule Tile(오토타일) 채택**. 사용자가 2D 게임 개발 경험이 있어 Unity Editor에서 Rule Tile 에셋을 직접 세팅 가능 — 이건 코드가 아니라 에디터 에셋 작업이므로 "코드 없이 진행" 원칙과 충돌 없음. **스펙 정리 시점은 지금이 아니라 실제 맵 제작 단계** — 맵을 만들면서 그때그때 필요한 조각을 짚어주거나, 사용자가 직접 타일을 보면서 판단하는 방식으로 진행하기로 함(미리 문서화 불필요).

## 4. 방 역할 태깅 규칙 (BSP 결과 위에 얹는 별도 패스)

BSP는 방의 "형태/배치/연결"만 만들고, 어떤 방이 상자방인지 보스방인지는 별도 태깅 규칙으로 정함. 아래는 확정된 규칙:

### 상자방 (= "열쇠방")

- **상자방은 무조건 단칸방(막다른 구조)** — 메인 동선에서 갈라지는 단일 연결 통로 하나로만 이어짐. 다른 방과 관통 연결되면 동선 낭비가 가속화된다는 판단 때문에, 상자방은 항상 dead-end 취급
- **일반 상자는 맵 전역(일반 방)에도 몇 개씩 기본으로 깔림** — 상자방 전용이 아니라 층 전체에 분산 배치
- **잠금 여부**: 일반 방의 상자도 잠긴 상자가 섞여 있을 수 있음(전부 열려있는 게 아님). 상자방 내 상자는 대체로 열려있는 편이지만 **6:4 비율로 일부는 잠겨있음**(대략 60% 개방 : 40% 잠김으로 해석 — 정확한 배분은 추후 튜닝)
- **보상 등급**: 잠긴 상자는 일반 상자보다 좋은 보상(반지, common 등급 이상 장비 등)이 나올 **확률이 높지만, 무조건은 아님** — 확률 가중치 방식(녹픽던 참조: 잠긴 상자가 좋은 아이템 확률은 높지만 보장은 아니었음)

**확정 (질문 H 해결)**: 상자방(=열쇠방)은 **입구 문도 잠길 수 있음** — 문 잠금과 상자 잠금(6:4 비율)이 별개로 존재하는 이중 구조가 맞음. 처음 우려했던 "이중 게이트라 불쾌함" 문제는 열쇠의 성격을 바꿔서 해결함 (아래 참고).

**해정 메커니즘 — 타르코프 방식(다회용 충전형 열쇠)으로 확정**:
- 열쇠는 **던전 입장 전에 미리 챙겨오는 다회용(N회 충전) 장비** — 층 안에서 찾아 헤매는 소모성 아이템이 아님. 문 열쇠 / 상자 열쇠는 별개 카테고리, 각각 독립적으로 충전 횟수 소모
- 열쇠가 없거나 충전이 떨어졌으면 **그 자리에서 X tick 소모로 자물쇠 따기**로 대체 가능
- 이전에 걱정했던 "이중 게이트로 인한 불쾌함"은 **열쇠를 층에서 찾아야 하는 탐색 리스크** 때문이었음 — 열쇠를 미리 챙겨오는 장비로 바꾸면서, 던전 안에서는 "충전을 쓸까 tick을 쓸까"라는 즉석 판단만 남고 탐색 실패의 억울함이 사라짐. 문/상자 잠금이 동시에 있어도 각각 독립적인 즉석 결정이라 문제 없음
- 상세 구조/미확정 세부수치(충전 횟수, tick 비용, 문 잠금 확률 등)는 `ITEM_SYSTEM_DESIGN.md` 4장 "열쇠(Key)" 참고

### 보스방

- **보스방은 일반 방보다 작게** — 맵 크기를 줄이고 보스 + 소수 enemy만 배치해서 "보스를 공략한다"는 느낌을 살리는 방향
- **설계 이유**: 보스가 있는 층은 보스를 처치해야 다음 층으로 넘어갈 수 있는 구조라, 보스방까지 일반 방처럼 크고 복잡하게 만들면 게임이 늘어져서 질리기 쉬움 — 보스방은 의도적으로 압축된 전투 공간

### 남은 것

- 문/복도 연결이 항상 1:1인지, 순환 구조(고리형 경로)가 허용되는지
- 밤 시스템과의 연동 — ITEM_SYSTEM_DESIGN.md에 "밤에는 몹 데미지 증가 + 다른 종류 등장"이 언급됐는데, 이제 실시계(2장 참고)가 생겼으니 **밤을 특정 시각 구간(예: 18:00~06:00)으로 정의**할 수 있을 것으로 보임 — 같은 맵에서 시간에 따라 바뀌는 건지, 별도 맵인지는 여전히 미정
- **시간 기반 이벤트** — 별도 문서 `ALTAR_AND_TIME_SYSTEM_DESIGN.md`에서 상세 기획 진행 중: 4단계 시간대(아침/낮/저녁/심야)별 콘텐츠 테이블 변화, 5층 보스+NPC 퀘스트를 통한 "월식의 제단"(던전 시간을 자정으로 고정) 해금 흐름까지 설계됨
- 몹/아이템 스폰 로직 — 이 문서는 어디까지나 "맵 구조(타일 배치)"만 다루고, 스폰은 별도 범위로 보임

## 5. 다음 액션

**미정 질문 A~F + 방 역할 태깅(상자방/보스방) + 잠금 메커니즘(H)까지 전부 해결 — 맵 제너레이터 구조 설계는 이걸로 완전히 마무리.**

1. 열쇠 충전 횟수(N)/자물쇠 따기 tick 비용(X)/문 잠금 확률 등 세부 수치는 실플레이 튜닝 단계에서 결정
2. ~~`floor_ladder` 용도 스크롤을 아이템 목록에 추가할지~~ — **해결.** `ITEM_SYSTEM_DESIGN.md`에 5번째 스크롤(`scroll_01e`)로 확정 추가됨
3. Rule Tile 스펙은 실제 맵 제작 단계에서 그때그때 논의 — 지금 미리 준비할 필요 없음
4. 홈PC 쪽 `FloorLayoutGenerator`/`FloorTilemapPainter` 작업이 이미 어느 정도 진행됐다면, 그 내용과 이 문서를 맞춰봐야 함 — 두 대화가 서로 다른 설계로 나뉘지 않도록 주의 필요
5. 남은 세부: 문/복도 연결이 1:1인지 순환 구조 허용인지
6. **이번에도 코드/씬 작업은 명시적 명령 전까지 진행하지 않음**
