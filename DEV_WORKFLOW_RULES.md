# 개발 워크플로 규칙 (Dev Workflow Rules)

> 이 문서는 **"코드 작업 대화"** (이 대화)에서 지켜야 하는 공용 작업 규칙을 고정해둔 것입니다.
> 2026-08-27, 사용자 요청으로 작성됨. 이후 사용자가 직접 읽고 수정하는 방식으로 갱신됩니다 — Claude가 임의로 규칙을 바꾸지 않습니다.

---

## 규칙 1 — 설계 우선 (Design-First)

Claude는 코드 작성을 요청받아도 **바로 코드를 쓰지 않는다.** 순서는 항상:

1. 설계안/구조를 텍스트로 먼저 제시
2. 사용자 피드백을 받아 조정 (필요하면 여러 번 왕복)
3. 합의된 뒤에만 실제 코드 작성

### 1-1. 네이밍 규칙
변수/함수/클래스명은 추상적으로 짓지 않는다. **처음 보는 사람도 이름만 보고 기능을 어느 정도 유추할 수 있는 수준**으로 직관적으로 작성한다.

- 나쁜 예: `Manager`, `Handler`, `Data`, `DoStuff()`
- 좋은 예: `TickManager`, `PlayerTurnScheduler`, `FloorLayout`, `AdvanceToNextActor()`

### 1-2. 문서화 규칙
코드 안에 **summary(파일 상단 설명 주석) + 인라인 주석**을 적극 사용해서 추적성과 이해도를 높인다.

- 파일 상단에 "이 파일이 뭘 하는지 / 왜 이렇게 짰는지"를 요약하는 주석을 남긴다 (기존 `DungeonTools` 스크립트들의 `// Dungeon Tools N) ...` 헤더 스타일을 계속 따름)
- 판단이 갈릴 수 있는 설계 결정(왜 이 방식을 택했는지, 어떤 대안을 버렸는지)은 주석으로 남긴다
- "무엇을 하는가"보다 "왜 이렇게 했는가"를 우선해서 주석에 담는다 (코드만 봐도 알 수 있는 걸 그대로 되풀이하지 않음)

---

## 규칙 2 — 작업 기록 & 리뷰 프로세스

코드 작성이 끝이 아니다. 아래 두 문서를 계속 갱신한다.

### 2-1. 작업 보고서 — `DEV_LOG.md`
날짜별로 어떤 기능을 추가/변경했는지 기록한다. git에 저장해서 노트북/홈PC 등 다른 세션에서도 이력을 확인할 수 있게 한다.

### 2-2. 코드 리뷰 & 기술부채 기록 — `CODE_REVIEW_LOG.md`
작업 보고서 작성 후 사용자와 코드 리뷰를 진행하고, 그 피드백/리뷰 결과를 별도 문서로 남겨 기술부채를 추적한다.

---

## 참고 — 브릿지/문서 충돌 주의

`DOC_STORAGE_README.md`에 따르면, 디바이스 브릿지(폴더 접근)는 **세션(대화)당 하나만 안정적으로 살아있을 수 있음** — 다른 대화(예: "노트북 기획 대화")가 같은 폴더를 물고 있으면 이 대화 쪽 파일 읽기가 HTTP 401로 막힐 수 있다. 이런 증상이 다시 보이면:

1. 다른 대화가 같은 폴더에 연결되어 있는지 확인
2. 필요시 그쪽 연결을 먼저 해제
3. 그래도 안 풀리면 GitHub(`https://github.com/mun0520j-alt/202202825`) 경유로 우회 (읽기: `git pull` 후 로컬 clone에서 읽기 / 쓰기: 디바이스 브릿지로 직접 쓰기는 별도 경로라 대체로 계속 동작함, 2026-08-27 기준 확인됨)
