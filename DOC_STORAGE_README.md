# 202202825 문서 저장용 (이 대화의 역할 안내)

> 이 대화(노트북 대화)는 **기획/설계 문서 전용**입니다. 코드나 씬 작업은 하지 않습니다.
> 이 파일은 "왜 이 폴더에 이런 문서들이 쌓이는지" 나중에 다른 세션/사람이 봐도 바로 이해하도록 만든 인덱스 겸 안내문입니다.

---

## 1. 이 폴더의 위치와 성격

- 경로: `C:\Users\munju\OneDrive\문서\202202825`
- **현재 이 경로 자체가 git과 연결되어 있음** (확인됨, 2026-08-27)
- Claude(이 대화)는 파일 읽기/쓰기 권한만 있고, git add/commit/push 같은 셸 명령 실행 권한은 없음
  → **문서 갱신은 Claude가 하지만, 실제 git commit/push는 사용자가 직접 수행**

## 2. 워크플로

1. 노트북 대화에서 기획 내용이 새로 확정되거나 변경될 때마다
2. Claude가 관련 `.md` 문서를 수정하고
3. `SendUserFile` + 디바이스 브릿지로 이 폴더에 바로 저장(덮어쓰기)
4. 사용자가 적절한 시점에 git commit/push로 이력 남김

## 3. 이 폴더에 저장되는 기획 문서 목록 (2026-08-27 기준)

| 파일 | 내용 |
|---|---|
| `PROJECT_PLAN.md` | 프로젝트 전체 진행 상황 Living Doc — 개요, 로드맵, Dev Sequence(0~7단계), 변경 이력 |
| `OPEN_QUESTIONS_MASTER.md` | 미정 항목 마스터 체크리스트 (우선순위별) |
| `MAP_GENERATOR_DESIGN.md` | 맵 제너레이터 설계 (BSP, 방 태깅, 열쇠 시스템 등) |
| `ITEM_SYSTEM_DESIGN.md` | 아이템 시스템 설계 (슬롯/무기/방어구/소비 아이템/열쇠) |
| `ALTAR_AND_TIME_SYSTEM_DESIGN.md` | 시간대 시스템 + 제단(Altar) 시스템 설계 |
| `SETTLEMENT_AND_MERCHANT_DESIGN.md` | 영지(거점) + 상인 + 연성로 시스템 설계 |
| `VERTICAL_SLICE_SCOPE.md` | 3개월 버티컬 슬라이스("Battle MVP") 범위 정의 |
| `HOMEPC_SYNC_NOTES.md` | 홈PC 대화 문서와의 대조/충돌 해소 노트 |
| `DOC_STORAGE_README.md` | (이 파일) 문서 저장 구조 안내 |

같은 문서들은 claude.ai **프로젝트 "202202825 main pc asan"**에도 동일하게 올라가 있어서, 다른 대화(홈PC/개발 등)에서 같은 프로젝트에 연결하면 폴더 접근 없이도 내용을 바로 볼 수 있음.

## 4. 대화 구조 (참고)

프로젝트는 총 3개 대화로 분리 진행 중:
1. **이 노트북 대화** — 순수 기획/설계, 코드 없음 (이 폴더에 문서 계속 쌓는 역할)
2. **홈PC 기획/아키텍처 대화** — `DUNGEON_RUNTIME_ARCHITECTURE.md` 등 런타임 구조 설계
3. **개발(코드 작성) 대화** — 실제 Unity 구현

> 주의: 디바이스 브릿지(폴더 접근)는 세션당 하나만 살아있을 수 있음 — 한 대화가 폴더를 붙잡고 있으면 다른 대화에서 같은 폴더 접근 시 HTTP 401이 날 수 있음. 이 경우 어느 대화가 폴더를 물고 있는지 확인 후 그쪽을 정리해야 함.
