# 아이템 시스템 설계 논의 문서

> 상태: **구조/카테고리 설계 완료.** 실제 데이터는 `ITEM_DATA_DRAFT.xlsx`(별도 파일)에 v1 초안으로 옮겨져 있음 — 이 문서는 "왜 이렇게 정했는지"를 담은 설계 근거 문서, xlsx는 "실제 수치"를 담은 데이터 소스.
> `DUNGEON_RUNTIME_ARCHITECTURE.md`와 같은 방식: 먼저 구조를 글로 합의 → 그 다음 실제 데이터표 작성. 이 프로젝트는 그 두 번째 단계까지 진행됨.

---

## 1. 장비 슬롯 구조 (확정)

| 슬롯 | 개수 | 비고 |
|---|---|---|
| 메인 무기 | 2 | 검/지팡이/활 중 자유 조합, 스왑 가능 |
| 보조 슬롯(오프핸드) | 1 | 방패 전용. 메인 무기 슬롯과 별개 |
| 방어구(상의) | 1 | 몸통 방어구 1종만 채용 (boots/gloves 등 스프라이트는 있으나 이번 범위 제외, 추후 업데이트 가능성 높음) |
| 장신구 - 반지 | 2 | 원본 녹픽던도 실제로는 반지 2슬롯 |
| 장신구 - 목걸이(Necklace) | 1 | **예비 휴대 불가 — 던전 입장 시 딱 1개만 소지 가능** (2장 참고) |
| 포션 / 스크롤 | 전용 슬롯 없음 | 일반 인벤토리(가방) 칸을 차지, 소지 중이면 즉시 사용 가능, **1회성 소모 아이템** |

## 2. 무기 & 방패

### 메인 무기 (검/지팡이/활)
- 2슬롯에 **검/지팡이/활을 자유롭게 조합**해서 장착
- **활(Bow) 선별 확정**: `bow_01a/b/cc` 3종만 사용. **화살(Arrow) 스프라이트는 사용하지 않음**

### 방패 (오프핸드 전용 슬롯)
- 공격/방어 액션 없음, 장착 시 **받는 피해 감소(flat, -1~-10 범위)** 만 적용
- 밸런스 의도: 6층 이상 몹 밀도 증가 + 밤 시간대 몹 강화를 감안해, 너무 강하지도 약하지도 않은 구간으로 설정

### 무기 스왑 & tick 비용
- **전투 중 스왑 = 1 tick 소모 / 비전투 중 스왑 = 무료**
- 설계 의도: 스왑은 "미리 준비하면 쓸 수 있는 선택지"이지 상시 무료 옵션이 아님. 전투 중 급하게 스왑하는 건 "준비 못한 대가"
- 1 tick이 상대적으로 비싼 비용이라 **시작 tick 자체를 높이는 방향**으로 상쇄 예정 (`DUNGEON_RUNTIME_ARCHITECTURE.md`와 연동, 수치 미정)

### 무기 스킬 시스템 (이 문서 범위 밖)
- 무기 종류별 스킬 풀에서 루팅 시 3개를 획득, 성능(데미지/범위)은 무기 등급에 따라 달라짐
- **스킬 데이터는 별도 엑셀 시트에서 관리** — 유지보수성 우선, 이 문서/데이터표에서는 다루지 않음

## 3. 장신구 (반지 / 목걸이) — 서로 다른 설계 원칙

### 반지 (등급이 아니라 "스탯 종류"로 구분, 확정)
- 반지는 등급별 스케일링이 아니라, **개별 아이템마다 서로 다른 스탯 하나를 부여**
- 5개 고정 스탯: **힘(공격력) / 체력 / 속도 / 방어력 / 치명타 확률**
- **치명타 확률은 반지로만 루팅 가능한 반지 전용 스탯** — 스킬트리 등 다른 경로로는 얻을 수 없음 (설계 포인트)
- Kyrise 아이콘에 3개 시리즈(01/02/03)가 있어, **시리즈 = 같은 스탯의 세기 단계**로 활용 (예: 힘 반지가 01/02/03 순으로 +3/+5/+7)
- 반지 15종 전부 채택 (3시리즈 × 5스탯)

### 목걸이 (반지와 완전히 다른 설계 — 강력한 고유 능력 + 엄격한 휴대 제한)
- **1시리즈 5종만 채택** (02/03시리즈는 미사용)
- 반지처럼 단순 스탯이 아니라 **각 5개가 서로 다른 강력한 고유 능력**을 가짐:
  1. 앙크의 목걸이 — 치명적 피해 사망 시 1회 한정 체력 1로 생존
  2. 도적의 목걸이 — 열쇠 없이 상자/잠긴 문 강제 개방 (N회)
  3. 감시자의 목걸이 — 상시 지속으로 시야 밖 몹 위치 표시
  4. 탈주자의 목걸이 — 스크롤 없이 어디서든 즉시 탈출
  5. 수집가의 목걸이 — 획득 골드/아이템 가치 % 증가
- **소모 방식 2종류**:
  - **사용 시 소모** (앙크/도적/탈주자) — 실제로 효과가 발동될 때만 횟수 차감
  - **장착 즉시부터 소모(경과형)** (감시자/수집가) — 착용한 순간부터 시간/런 경과에 따라 자동으로 차감, 안 써도 줄어듦
- **휴대 제한(확정, 핵심 규칙)**: 던전 입장 시 목걸이는 **딱 1개만 소지 가능** — 예비를 인벤토리에 넣어두는 것도 불가. 착용 중 다른 목걸이를 루팅하면 스왑은 가능(기존 것은 버려짐)하지만, 동시에 2개를 갖고 있을 수는 없음
- **획득 확률/루트는 일반 루팅 테이블보다 훨씬 희박** — 효과가 강력한 만큼 입수 장벽도 높게 설계
- 실제 수치(사용 횟수/지속 run·tick 수)는 v1 초안 상태, `ITEM_DATA_DRAFT.xlsx`의 Necklace 시트 참고

## 4. 소비 아이템 (포션 / 스크롤)

### 포션 — 8종 확정, `potion_01a~h` 스프라이트 사용 확정
1. 힐 포션 — 체력 회복 (즉시)
2. 방어력 포션 — 일정 tick 동안 방어력 증가
3. 힘 포션 — 일정 tick 동안 공격력 증가 (임시 버프, 영구 스탯업 아님)
4. 은신 포션 — 일정 tick 동안 은신
5. 데미지 포션 — 대상 지정 후 투척, 유일한 공격형 포션 (냉기/화염/독가스/마비가스를 하나로 통합)
6. 정신시야 포션 — 몹 위치 확인
7. 신속 포션 — 일정 tick 동안 이동속도 증가
8. 순수 포션 — 디버프 제거 / 유해 효과 면역

- 지속 버프형의 지속시간은 **tick 단위**
- "tick 회복 포션" 후보는 최종 8종에서 제외

### 스크롤 — 4종 확정, `scroll_01a~d` 스프라이트 사용 확정
1. 석화 스크롤 — 대상 몹을 일정 tick 동안 석화
2. 강제 탈출 스크롤 — 던전에서 즉시 탈출 (베이스캠프 귀환)
3. 맵 랜덤 이동 스크롤 — 같은 층 내 무작위 순간이동
4. 장비 변환 스크롤 — 장비를 같은 종류의 다른 아이템으로 변환

- `ScriptableObject` 기반으로 설계해서 이후에도 새 효과 추가 가능하게 유지 (유지보수성 우선)

### 참고 — 녹픽던(Shattered Pixel Dungeon) 스크랩 비교 결론
- 회복/신속은 그대로 대응, 냉기·화염·독가스·마비가스(투척형 4종)는 **데미지 포션 하나로 통합**해서 채택
- **힘(영구 스탯업) / 강화(아이템 등급 상승) 포션·스크롤은 채택 안 함** — 우리는 "장착해도 스탯만 변화, 아이템 자체는 불변" 원칙이라 아이템을 통한 영구 강화 개념이 설계와 충돌. 진행(성장) 축은 스킬트리로 분리됨
- 텔레포테이션(층내 이동)은 채택, 단 **탈출(던전 완전 이탈) 스크롤은 원작에 없는 우리 게임 고유 아이디어** — 익스트랙션 장르 특유의 요소

### 열쇠(Key) — 철회됨 (재검토 필요)

한때 "잠긴 상자는 열쇠 또는 X tick 자물쇠 따기로 개방" 구조를 확정했었으나 **철회**. 이유: 상자방(=열쇠방) 자체가 "잠긴 방"이라는 진입 장벽 개념과 겹칠 경우, **잠긴 방 안에 또 잠긴 상자가 있는 이중 게이트 구조**가 되어 체감이 불쾌할 수 있다는 문제 제기. 288 tick이라는 빠듯한 예산 안에서 이중으로 tick/열쇠를 요구하는 건 피로도가 너무 높을 수 있음.

**재검토 대상** (`MAP_GENERATOR_DESIGN.md` 4장의 "잠긴 방" 개념과 함께 다시 설계 필요):
- 상자방(=열쇠방) 진입 자체를 잠글지 여부 — 지금까지는 "단칸 dead-end 구조"라고만 했지 "입구가 잠겨있다"고 확정한 적은 없었음, 이 부분부터 재확인 필요
- 개별 상자 잠금(6:4 비율)은 유지할지, 이것도 재검토 대상인지
- 열쇠 아이템/자물쇠 따기 메커니즘 자체를 아예 다른 방식으로 대체할지 (예: 잠금 없이 상자방 안 상자는 전부 확률 기반 보상 등급만 다르게)

## 5. Tick 자원 개념 (아키텍처 문서와 정합성 확인 완료)

- "tick 최대치" = **시작 tick**(해당 런을 시작할 때 갖는 tick 값)
- 회복형 효과(tick 회복 등)는 시작 tick을 초과해서 증가하지 않도록 캡
- 별도의 "스태미나 풀" 개념이 아니라, 시간-비용 누적 모델 위에 "회복은 시작값까지만"이라는 상한 규칙이 추가된 것 — 아키텍처 문서와 충돌 없음

## 6. 플레이어 체력

- **체력은 고정 시스템** — 시작 체력 = **100**
- 이후 **스킬트리를 통해 어느 정도 증가 가능** (증가폭/스킬트리 구조는 미논의, 별도 문서 대상)

## 7. 데이터 정밀도 방침

- **이름/설명**: 약식으로 정리
- **효과/스탯 수치**: 약식 대상 아님 — 실제 의미 있는 값(v1 초안)으로 정리, **실플레이하면서 조정**

## 8. 카테고리 ↔ 슬롯 매핑 (확정)

| 아이콘 카테고리 | 슬롯 |
|---|---|
| 검(Sword) 3시리즈 전체 (15종) | 메인 무기 |
| 지팡이(Staff) 02시리즈 (5종) | 메인 무기 |
| 활(Bow) 01 a/b/cc (3종) | 메인 무기 (화살 미사용) |
| 방패(Shield) 02시리즈 (5종) | 보조 슬롯(오프핸드) |
| 갑옷(Armor) 01시리즈 전체 (5종) | 방어구(상의) |
| 반지(Ring) 3시리즈 전체 (15종) | 장신구 - 반지 |
| 목걸이(Necklace) 01시리즈만 (5종) | 장신구 - 목걸이 |
| 포션(Potion) 01시리즈만 (8종) | 소비 아이템 |
| 스크롤(Scroll) 01a~d (4종) | 소비 아이템 |

## 9. 남은 미정 항목

1. **각 카테고리 스탯의 정확한 수치** — `ITEM_DATA_DRAFT.xlsx`에 v1 초안으로 채워져 있음. 실플레이 밸런싱 전 시작점이며 확정 아님
2. **방패/갑옷/반지/목걸이의 5등급(또는 5종) 스케일 자체도 v1 임의값** — 사용자가 "등급 체계는 상관없다"고 해서 A안(스프라이트 5개=5등급 자연 대응)으로 임의 진행함. 이견 있으면 조정 가능
3. ~~시작 tick 값 상향 폭~~ — **해결됨.** 원 설계 당시 정했던 수치를 확인: **1 Tick = 게임 내 5분 = 이동 5타일, 시작(최대) tick = 288 = 24시간**. 자세한 내용은 `MAP_GENERATOR_DESIGN.md` 2장 참고. 행동해야만 tick이 흐르고(메뉴 확인 등은 무료), UI는 남은 tick보다 실시계+잔여시간 표시 예정
4. **무기 스킬 시스템 전체** — 완전히 별도 엑셀/문서로 진행 예정, 착수 시점 미정
5. ~~전투(공격) 1회당 tick 비용~~ — **해결됨.** 공격 = 1 tick 확정 (이동 5타일=1tick, 스왑=1tick과 동급). 포션/스크롤 사용, 상자/문 개방 등도 전부 1 tick으로 통일 확정 — 사실상 게임 내 모든 행동의 기본 단위가 1 tick. 자세한 내용은 `MAP_GENERATOR_DESIGN.md` 참고
6. **5층 보스 + 제단 시스템** — 새로 시작된 기획 스레드, `ALTAR_AND_TIME_SYSTEM_DESIGN.md` 참고. 시간대(아침/낮/저녁/심야)별 콘텐츠 변화 + 첫 제단("월식의 제단") 해금 퀘스트 흐름까지 설계됨. 이 문서(아이템)와의 접점: 퀘스트 아이템(가칭 "월식의 파편")의 실제 데이터/스프라이트는 아직 미배정
7. ~~열쇠(Key) 카테고리~~ — **철회됨.** 잠긴 방 + 잠긴 상자 이중 게이트 우려로 재검토 중, 4장 참고
8. **"다음 층 직행" 스크롤 추가 여부** — `MAP_GENERATOR_DESIGN.md`에서 `floor_ladder` 전용으로 언급된 신규 스크롤(탈출 미지원, 다음 층行만). 확정 4종 스크롤 목록에 5번째로 넣을지 확인 필요

---

## 다음 액션

1. `ITEM_DATA_DRAFT.xlsx`의 v1 스탯 초안을 실플레이하면서 조정
2. ~~시작 tick 상향 폭 결정~~ 완료 (288tick = 24시간, `MAP_GENERATOR_DESIGN.md` 참고) — Player_Base 시트에 반영 필요
3. 승인되면 Unity `ScriptableObject`/데이터 파일로 옮기는 작업 착수 (사용자 명령 대기 — 코드 작성 전 반드시 확인)
4. 무기 스킬 시스템은 별도 엑셀로 착수 시점에 다시 논의
