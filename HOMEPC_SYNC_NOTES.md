# 홈PC ↔ 노트북 문서 대조 노트

> 목적: 홈PC 대화의 `DUNGEON_RUNTIME_ARCHITECTURE.md`(Tick/이동/카메라 런타임 설계)와 노트북 대화에서 확정한 내용(아이템/맵/시간대) 사이에 충돌이 없는지 대조. `OPEN_QUESTIONS_MASTER.md` 4순위 항목의 결과물.
> **이 문서는 노트북 쪽 세션에서 작성됨 — 홈PC 대화 쪽에는 자동 반영 안 됨. 아래 내용, 특히 "1. 핵심 충돌"은 홈PC 대화에도 직접 전달해서 그쪽 문서와 맞춰야 함.**

---

## 1. 핵심 충돌 — Tick/이동 비용 모델이 서로 다름 (중요)

**홈PC 문서(1.3절)의 전제**: 녹픽던(Shattered Pixel Dungeon) 참고, **배율 기반 연속 모델**
- 기본 이동 속도 = 1 (1타일 이동 = 1턴 소모)
- 기본 공격 속도 = 1 (1회 공격 = 1턴 소모)
- 스탯/아이템으로 배율이 붙어서 "행동 1회 = 기본 비용 × (1/속도배율)"로 시간 소모가 줄어드는 구조 (예: Freerunner 이동속도 1.6배)

**노트북에서 확정한 모델**: `MAP_GENERATOR_DESIGN.md` 2장, **고정 단위 기반 모델**
- **1 Tick = 게임 내 5분 = 이동 5타일** (1타일=1틱이 아니라 **5타일 이동에 1틱**)
- **기본 공격 = 1 tick**, 무기 스왑(전투 중) = 1 tick, 포션/스크롤 사용 = 1 tick, 상자/문 개방 = 1 tick — **게임 내 모든 기본 행동이 "1회 = 1 tick"으로 통일**
- 배율(속도 스탯이 tick 비용 자체를 줄이는) 개념은 노트북 쪽에서 명시적으로 도입한 적 없음 — 오히려 "행동의 가치를 계산해보면 1tick이 적당하다"는 판단으로 **고정값 통일**을 택함
- 스킬만 예외: 스킬은 스킬마다 X/Y/Z tick으로 다른 고정 비용을 가짐(역시 배율이 아니라 스킬별 고정값)

**충돌 지점**: 홈PC 쪽 미정 질문 C("속도 스탯을 배율로 할지, 이동/공격 비용을 분리할지")는 애초에 "배율 기반 모델"을 전제로 한 질문인데, 노트북 쪽은 이미 "고정 1tick 통일 + 이동만 5타일 단위"로 못박아버려서 **전제 자체가 달라짐**. 이대로 두면 두 대화가 서로 다른 경제 모델로 각자 구현을 진행하게 됨.

**해결됨(사용자 확인)**: 홈PC 문서 1.3절의 "배율"은 실제 tick 비용을 바꾸는 게임플레이 스탯이 아니라 **카메라 배율(줌/스케일) + 실제 타일 단위(5칸=1tick)**를 가리키는 것으로 확인됨. 즉 게임 로직상 시간 비용은 어디까지나 **노트북에서 확정한 고정 모델(모든 기본행동=1tick, 이동=5타일/1tick)** 하나로 통일되고, 홈PC 문서의 "배율" 언급은 시각적/카메라 표현 쪽 얘기였던 것으로 정리됨.

⚠️ **다만 주의**: 홈PC 문서 1.3절 원문은 "Freerunner 클래스 이동속도 1.6배", "Ring of Haste" 등 **명시적으로 게임플레이 속도 스탯이 시간 비용을 줄이는 예시**를 들고 있어서, 문면 그대로 읽으면 배율 기반 턴 비용 모델처럼 보임. 이게 카메라 배율 얘기였다면 홈PC 문서 자체의 표현이 오해를 부르기 쉬우니, **홈PC 대화에서 1.3절 문구를 명확하게 다시 정리해두는 걸 권장** — "이동/공격 속도 관련 배율은 카메라/시각 표현에만 적용되고, tick 비용 자체는 고정 모델을 따른다"는 식으로.

## 2. 엔진 핵심 질문 4개 — 전부 해결됨 (노트북 대화에서 결론)

이전엔 "노트북 쪽에서도 명시적으로 정한 적 없다"고 남겨뒀던 항목들. 개발 착수 전에 노트북 대화에서 미리 결론 내림:

1. **Tick 이벤트 모델 — 큐 기반 스케줄러로 확정**: TickManager가 각 엔티티의 "다음 행동 시점"을 큐로 관리하고, 차례가 된 엔티티에게만 직접 콜백. 전체 브로드캐스트 방식 아님. 스킬(X/Y/Z tick 가변비용)과도 가장 자연스럽게 맞물림 (홈PC 미정 질문 B 해결)
2. **이동 tick 비용 — 누적형으로 확정**: 1타일 이동마다 0.2tick씩 누적되다가 정수(1)가 되는 시점에 실제 게임 시간이 진행됨. "5타일 채워야만 1tick"(배치형) 아님
3. **이동 입력 — 클릭+경로탐색 자동이동으로 확정**: 홈PC 문서 3장의 녹픽던 참고 방식 그대로. 클릭한 타일까지 A* 등으로 자동 경로탐색 이동, 도중 상황 변화 시 중단 (홈PC 미정 질문 H 해결)
4. **공격 트리거 — 적 칸 클릭 시 자동 공격 전환으로 확정**: 이동과 동일한 클릭 UX, 별도 공격 입력 불필요 (홈PC 미정 질문 I 해결)

이 4개는 홈PC 문서의 미정 질문 B/H/I 및 노트북 쪽 파생 문제를 한 번에 해결함 — **홈PC 대화에도 이 결론을 반영해서 문서 갱신 필요**.

## 3. 충돌 아님 — 서로 보강되는 부분

- **엔티티가 전부 Tick을 구독**(홈PC 미정 질문 D)는 노트북의 "모든 행동=1tick 통일" 원칙과 자연스럽게 맞음 — 몹도 동일한 tick 단위를 쓴다고 가정하는 게 일관적. 확정까지는 아니지만 방향은 일치
- **던전 탈출 시 런 데이터 이전**(홈PC 미정 질문 G) — 노트북 쪽에서 "죽거나 MIA되면 아이템/퀘스트아이템 손실"이 이미 여러 번 확정됨(월식의 파편 등). 즉 "탈출 성공 = 인벤토리 유지, 사망/MIA = 손실"이 이전해야 할 런 데이터의 규칙으로 이미 정해져 있음 — 홈PC 쪽 구현 시 참고 가능
- **10개 층 사전 생성**(홈PC 2.1절)과 노트북의 BSP 절차생성은 충돌 없음 — "DungeonScene 진입 시 10개 층을 각각 BSP로 생성해서 미리 world-space에 배치"로 조합 가능
- **층 크기가 균일하지 않음**(노트북 확정: 1~5층은 축소 규격, 6층+는 v4 24x24 풀사이즈) — 홈PC의 미정 질문 E(층 배치를 world-space에 어떻게 늘어놓을지, 예: X축 나열)에 실질적 힌트가 됨: **층마다 크기가 다르므로, 배치 방식은 균일한 그리드 반복이 아니라 가변 크기를 감안한 배치**여야 함. 이것도 최종 결정은 아니고 참고용 제약조건

## 3.5. 워크플로 변경 (참고)

이 시점부터 프로젝트 진행이 **총 3개 대화**로 나뉨: (1) 이 노트북 대화(기획 전용, 코드 없음), (2) 홈PC 기획/아키텍처 대화, (3) 실제 개발(코드 작성) 대화. 노트북 대화는 계속 순수 기획 문서 축적 역할만 담당하고, 실제 구현은 (2)/(3) 쪽에서 진행.

## 4. 노트북 쪽에서 홈PC 문서에 없던 새 확정 사항 (참고용 목록)

홈PC 문서 작성 시점 이후 노트북에서 추가로 확정된 것들 — 필요 시 홈PC 쪽 아키텍처 설계에도 반영:
- 288 Tick = 24시간 = MIA 상한 (던전 최대 체류시간)
- 5층 = 첫 보스 전환점 (제단지기 NPC, 이후 게임이 "규칙을 배우는 구간"과 "규칙을 조작하는 구간"으로 나뉨)
- 자정(00:00) 기점으로 몹 강화(×1.3)/전용 몹/플레이어 시야 축소가 발동하는 시간대 시스템
- 제단(월식의 제단 등) — Dungeon Time을 특정 상태로 고정하는 승천형 난이도 조작 시스템

---

## 다음 액션

1. ~~1번(핵심 충돌)~~ — **해결.** "배율"은 카메라/시각 표현 얘기였던 것으로 확인, tick 비용은 고정 모델로 통일. 다만 홈PC 문서 1.3절 원문 표현이 오해 소지가 있어 그쪽에서 문구 정리 권장(위 참고)
2. ~~2번(이동 입력-tick 정합성 등 엔진 4개 질문)~~ — **해결.** 위 2절 참고, 홈PC 대화에 전달 필요
3. 그 외 항목은 실제 구현 착수 시점에 참고
4. 실제 개발은 별도 대화(3개 중 (2)/(3))에서 진행 — 이 문서는 그쪽에 전달할 참고자료
