# 버티컬 슬라이스 범위 정의 — "Battle MVP" (약 3개월)

> 상태: **범위 + 엔진 핵심 질문까지 전부 확정, 개발 착수 가능**. 코드 없음(이 문서 자체는 여전히 기획 문서). 목표는 3개월 안에 "던전 플레이의 핵심 뼈대"만 완성하는 것 — 시간대/제단/영지/상인 같은 메타 시스템은 전부 이 슬라이스 밖.
> 근거: `PROJECT_PLAN.md` 6장 "개발 착수 순서(0~7단계)" 중 **0~5단계**가 이 버티컬 슬라이스의 범위와 정확히 겹침. 6~7단계(시간대·제단, 영지·상인)는 이번 3개월 목표에서 완전히 제외.

---

## 1. 목표 (사용자 정의)

**Battle MVP** — 아래 3가지 상호작용이 실제로 돌아가는 최소 던전 플레이 루프:
1. **플레이어 ↔ Enemy 상호작용** (이동, 전투)
2. **층을 올라가는 구조** (맵 생성, 계단, 층 전환)
3. **아이템 루팅** (드랍, 획득, 장착)

**모든 구조를 다 만드는 게 아니라, 위 3개가 성립하는 최소 단위**를 목표로 함.

## 2. 범위 안 (In Scope) — 제안

| 시스템 | 범위 | 근거 문서 |
|---|---|---|
| Tick/이동 엔진 | 그리드 이동, tick 소모(이동=5타일/1tick, 공격=1tick) | `MAP_GENERATOR_DESIGN.md` 2장 |
| 전투 | 기본 공격, 데미지 계산(무기 스탯 기반), **몹 2~3종**(스탯만 다르고 행동패턴은 동일한 단순 버전) | 신규(스킬 시스템은 제외, 아래 참고) |
| 몹 스폰 | 몹 리스트를 index 0~2로 두고 **랜덤 index 추첨**으로 소환 | 신규 |
| 맵 생성 | BSP 방+복도, `floor_stairs`로 다음 층 이동, 288tick MIA 카운트다운 | `MAP_GENERATOR_DESIGN.md` |
| 아이템 — 장비 | 무기(검/지팡이/활) + 방패 + 상의, 장착 시 스탯 반영 | `ITEM_SYSTEM_DESIGN.md` 1~2장 |
| 루팅 | 상자에서 아이템 드랍(잠금 없는 일반 상자만) | `MAP_GENERATOR_DESIGN.md` |
| 탈출 | 계단에서 "탈출" 선택 시 성공, 288tick 초과 시 MIA(아이템 손실) | 기존 확정안 |

## 3. 범위 밖 (Out of Scope — 이번 3개월엔 안 함) — 제안

| 시스템 | 이유 |
|---|---|
| 반지(Ring) | 5스탯 종류 — 확정: **추가 업데이트로 연기**, 이번 슬라이스엔 미포함 |
| 포션(Potion) | 8종 — 확정: **추가 업데이트로 연기**, 이번 슬라이스엔 미포함 |
| 목걸이(Necklace) | 고유능력 5종 + 휴대 1개 제한 + 소모방식 2종 — 로직이 복잡해서 후순위 |
| 스크롤(Scroll) 5종 | 효과가 제각각(석화/탈출/순간이동/변환/직행) — 전투 루프 검증엔 필수 아님 |
| 열쇠(Key) + 잠긴 상자/문 | 자물쇠 따기 tick 로직, 상자방 태깅까지 포함 — 맵 생성 기본 골격만 먼저 검증 |
| 무기 스킬 시스템 | 별도 엑셀로 관리 예정, MVP는 기본 공격만으로 전투 루프 검증 |
| 시간대 시스템(자정 등) | Step 6, 메타 시스템 |
| 제단(Altar)/연성로 | Step 6~7, 메타 시스템 |
| 영지/상인 | Step 7, 메타 시스템 |
| 1~5층 vs 6층+ 규격 이원화 | 일단 한 가지 규격으로 통일해서 루프부터 검증, 층별 스케일링은 나중에 |

## 4. 확인 필요한 것 (사용자 판단 대기)

1. ~~반지/포션까지 이번 슬라이스에 포함할지~~ — **확정: 제외, 추가 업데이트로 연기**. 이번 슬라이스는 **무기+방패+상의**만으로 전투 루프 검증
2. ~~몹 종류~~ — **확정: 2~3종**. 행동패턴은 동일, **기본 스탯만 다르게** 차등(난이도 감각용). 소환 로직도 간단하게 **몹 리스트 index 0~2 중 랜덤 추첨**으로 확정
3. ~~엔진 0단계 질문 4개~~ — **전부 해결.** 큐 기반 스케줄러, 이동tick 누적형, 클릭+경로탐색 자동이동, 공격 자동전환으로 확정 (`HOMEPC_SYNC_NOTES.md` 2절 참고)

## 5. 다음 액션

1. 위 범위 확인/수정
2. 확인되면 `PROJECT_PLAN.md` 6장 Dev Sequence 0단계부터 순서대로 착수 (실제 코드는 별도 개발 대화에서)
3. 이 문서도 코드/씬 작업 없음 — 범위 합의까지만
